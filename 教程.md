# 抖店订单管家 · 使用教程

一篇从零上手的操作指南。读完约 5 分钟，按步骤走一遍即可日常使用。

> 适用版本：抖店订单管家 1.0.0（Windows / Android / iOS 三端界面一致）。

---

## 一、第一次打开

1. 安装并打开 App（Windows 双击 exe；Android 装 APK；iOS 通过 TestFlight / App Store）。
2. **未配置云端时**，App 会自动进入「本地模式」——所有功能（接抖店、录单、发货、导出）都能用，只是数据只存在本机这一台设备上。
3. 想多台设备共享同一份数据，再做下面的「连接云端」。

> 本地模式足够小商家日常使用；想换电脑也能看到历史订单，再接 Supabase。

---

## 二、连接云端（可选，多端同步才需要）

1. 准备一个 Supabase 项目（免费即可），在 SQL Editor 里执行本项目 `supabase/schema.sql` 建好数据表。
2. 打开 App → **设置 → 云端账号（Supabase）**。
3. 填入项目的 **URL** 和 **Anon Key**，点「保存配置」。
4. **重启 App** 让配置生效，然后用邮箱注册 / 登录。
5. 之后在任意设备登录同一账号，数据会自动多端同步。

> 不会填？把 URL 和 Key 直接写进 `lib/core/config/app_config.dart` 的默认值里，打包时分发出去，用户开箱即用。

---

## 三、接入抖店，让订单自动进来

1. 到抖店开放平台创建应用，拿到 **App Key / App Secret**；自用型应用还要填 **店铺 ID**。
2. App → **设置 → 抖店开放平台**，依次填写三项，点「保存并测试」。
3. 测试通过后会自动换取访问令牌（存本机，不落库）。
4. 打开「自动同步订单」开关，设定同步周期（默认 5 分钟）。
5. 点「立即同步」可手动拉一次；之后新增和变更的订单会按周期自动进来。

> 订单只走官方开放平台 API（签名 + 授权令牌），不爬页面。同步水位线向前重叠 10 分钟，避免漏单。

---

## 四、订单日常操作

- **看订单**：首页/订单页按状态、关键字、日期筛选；可切换排序（最新下单 / 金额高低）。
- **录线下单**：订单页「新建订单」，填收件人、加商品行（从商品档案带出价格），保存即可。
- **发货**：
  - 单笔：进订单详情 → 填物流公司 + 快递单号 → 标记发货（抖店订单会回写平台）。
  - 批量：订单页「批量发货」，一次处理多单，回传失败的会单独列出。
- **待发货预警**：超过设定小时数（默认 24h）未发货会高亮，方便催发。

---

## 五、商品 / 客户 / 售后

- **商品**：先建商品档案（名称、规格、商家编码、成本、售价、库存）。建单时直接选，价格自动带出；订单毛利靠成本算。
- **客户**：抖店订单同步时自动归档买家（按 open_id / 手机号去重），可加备注、看历史订单。
- **售后**：退款 / 退货 / 换货 / 补发统一管理，记进度与退款金额；未结单在首页有红点提醒。

---

## 六、看统计

打开「统计」页，可看：
- 销售额（今日 / 本月 / 累计）、待发货数、售后未结金额；
- 近 6 个月销售额走势；
- 商品销量 Top（按销量 / 金额）。

数据都在本机内存实时算，断网也能看。

---

## 七、导出 Excel（对账 / 盘点）

1. **设置 → 数据导出 → 导出 Excel**。
2. 桌面 / 安卓会弹出目录选择，挑个位置保存。
3. iOS 会走系统分享面板，选「存储到文件 / 微信」等目标即可。
4. 导出的文件含 5 个 sheet：**订单、订单明细、商品、客户、售后**，列对齐云端字段，状态用中文，金额可直接在 Excel 里求和。

---

## 八、常见问题

| 现象 | 处理 |
| --- | --- |
| 打开进本地模式、没有云端选项 | 在「设置 → 云端账号」填 URL/Key 并重启；或让打包方写死默认值 |
| 抖店同步没反应 | 检查 App Key/Secret/店铺 ID 是否正确，点「保存并测试」；确认已开「自动同步」 |
| 导出的 Excel 打不开 | 用 Excel / WPS / Numbers 打开；文件是标准 .xlsx |
| 换电脑看不到历史订单 | 之前在本地模式，数据只在本机；接好云端并登录同一账号后会同步过来（本地模式数据需手动迁移） |
| iOS 找不到「保存到文件夹」 | iOS 不支持目录选择，用分享面板选目标即可 |

---

## 九、给开发者的话

- 架构、环境要求、构建打包见 `README.md`；
- 云端表结构见 `supabase/schema.sql`；
- 三端打包见 `docs/build_windows.md`、`docs/build_android.md`、`docs/build_ios.md`。
